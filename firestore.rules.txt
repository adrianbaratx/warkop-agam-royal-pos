rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      // KHUSUS TESTING / DEVELOPMENT.
      // Jangan gunakan aturan ini untuk produksi karena semua orang bisa baca/tulis.
      allow read, write: if true;
    }
  }
}
