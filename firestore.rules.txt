rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      // KHUSUS TESTING / DEVELOPMENT.
      // Jangan pakai rules ini untuk produksi.
      allow read, write: if true;
    }
  }
}
